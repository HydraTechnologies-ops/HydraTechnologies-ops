# .github/labeler.yml

# List of labels and corresponding minimatch globs
apps: Apps/*
labels:
  - label: bug
    globs:
      - '*bug*'
      - '*error*'
  - label: feature
    globs:
      - '*feature*'
      - '*enhancement*'

# End of configuration
